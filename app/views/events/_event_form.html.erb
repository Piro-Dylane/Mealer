<div class="container">
  <%= simple_form_for (event), html: { class: 'd-none', data: { category_card_target: 'form' } } do |f| %>
    <%= f.input :title %>
    <%= f.input :description %>
    <%= f.input :date %>
    <%= f.input :time %>
    <%= f.submit class: 'btn btn-success' %>
  <% end %>
</div>
