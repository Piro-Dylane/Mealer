<div class="return-arrow-category">
  <%= link_to event_path(@event) do %>
    <i class="fas fa-arrow-left"></i>
  <% end %>
</div>

  <h1 class="chat-title"><%= @chatroom.event.title %> <img class="fruit-icon" src="https://img.icons8.com/color/48/000000/hamper.png" alt="icon of a fruit basket"/> </h1>

<div class="container col-8 test">
<div class="message-box-container">
<div class="event-container">
  <div id="messages" data-chatroom-id="<%= @chatroom.id %>">
    <% @chatroom.messages.each do |message| %>
      <%= render 'messages/message', message: message%>
    <% end %>
    </div>
    </div>
  <%= simple_form_for [@event, @chatroom, @message ], remote: true do |f| %>
  <div class="d-flex">
       <%= f.input :content, label: false, placeholder: "Write #{@chatroom.event.title}" %>
       <%= button_tag type: 'submit', class: "button-postfix" do %>
      <i class="fas fa-paper-plane" aria-hidden="true"></i>
    <% end %>
  </div>
<% end %>
</div>
</div>
